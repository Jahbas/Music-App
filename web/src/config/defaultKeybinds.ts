import type { KeybindMap } from "../types";

export const DEFAULT_KEYBINDS: KeybindMap = {
  playPause: ["Space"],
  volumeUp: ["ArrowUp"],
  volumeDown: ["ArrowDown"],
  nextTrack: ["N"],
  previousTrack: ["P"],
  toggleShuffle: ["S"],
  cycleRepeat: ["R"],
  openSearchOverlay: ["Slash"],
};

